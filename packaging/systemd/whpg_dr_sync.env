# Common environment for whpg_dr_sync (Primary or DR)
# Adjust paths for your environment.

# Path to config JSON
WHPG_DR_SYNC_CONFIG=/data/archive/dr_sync/dr_sync_config.json

# Optional: set a working directory (where logs/state live, if you choose)
WHPG_DR_SYNC_WORKDIR=/var/lib/whpg_dr_sync

# Optional: extra CLI args
WHPG_DR_SYNC_ARGS=

# Optional: set PATH if needed (Greenplum tooling, psql, gpssh)
PATH=/usr/local/bin:/usr/bin:/bin:/usr/local/greenplum-db-7.3.0-WHPG/bin
